(traffic-lights)=
# Traffic Lights

The traffic light system. This page is the worked example for the per-system Diátaxis
structure — copy this layout when adding a new system.

The four categories are listed explicitly (not globbed) so they stay in Diátaxis order:

```{toctree}
:maxdepth: 1
:hidden:
traffic_lights/tutorials
traffic_lights/how_to
traffic_lights/reference
traffic_lights/explanation
```
